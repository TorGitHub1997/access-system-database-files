create or replace function RemoveUser(cardid_number char(4))
returns bool as
$$
begin
	delete from userdata
	where card_id = cardid_number;
	if found then
		return 1;
	else
		return 0;
	end if;
end
$$
language plpgsql;
